<%= form_for(@blog) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="new_blog_form">
    <%= f.text_area :title, placeholder: "Title!", rows: 5, cols: 35 %>
    <br/>
    <%= f.text_area :content, placeholder: "Content!", rows: 10, cols: 50 %>
    <br/>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
