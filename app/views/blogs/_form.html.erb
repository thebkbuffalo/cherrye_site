<%= form_for(@blog) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="new_blog_form">
    <div class="new_blog_title">
      <%= f.text_area :title, class: "edit", placeholder: "Title!", rows: 5, cols: 35 %>
    </div>
    <br/>
    <div class="new_blog_content">
      <%= f.text_area :content, class: "edit", placeholder: "Content!", rows: 10, cols: 50 %>
    </div>
    <br/>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
