<%= form_for(@biography) do |f| %>
  <% if @biography.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@biography.errors.count, "error") %> prohibited this biography from being saved:</h2>

      <ul>
      <% @biography.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h1><%= f.label :bio %></h1><br>
    <%= f.text_area :bio, class: "edit" %>
  </div>
  <div class="field">
    <h1><%= f.label :press %></h1><br>
    <%= f.text_area :press, class: "edit" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
